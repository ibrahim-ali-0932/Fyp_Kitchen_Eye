[mutmut]
paths_to_mutate=services/stats_service.py
backup=False
runner=python -m pytest tests/test_stats_service.py -x --tb=short
tests_dir=tests/

[tool:pytest]
testpaths = tests

[coverage:run]
source = services
